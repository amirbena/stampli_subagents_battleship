# Pipeline reports — generated artifacts, not source code
reports/

# Presentation workspace — local-only generated material, never staged or committed
presentation/

# Dependency lockfiles — local-only, never committed
package-lock.json
**/package-lock.json

# Environment
.env
.env.local
.env*.local
apps/backend/.env

# Secrets
*.pem
id_rsa
id_ed25519

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.iml

# Playwright generated outputs — local artifacts, never staged or committed
apps/frontend/playwright-report/
apps/frontend/test-results/
