# Diff Scope Check

Workflow Run ID: 20260625-154353-0959d3e

README.md: install section split into 1a (frontend) + 1b (backend); chmod + mvn alternative added to 1b macOS block.
git rm --cached: 25 report files removed from index (disk intact, .gitignore already had reports/).

No production code, config, test, or infra files changed. package-lock.json not staged.

Result: PASS — in scope.
